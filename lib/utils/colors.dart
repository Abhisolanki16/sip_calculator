import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Prevent instantiation

  // ===== Brand / Accent =====
  static const Color primary = Color(0xFF137FEC);

  // ===== Backgrounds =====
  static const Color backgroundLight = Color(0xFFF6F7F8);
  static const Color backgroundDark = Color(0xFF101922);

  // ===== Dark UI (Finance App) =====
  static const Color appBackground = Color(0xFF1A202A); // Main background
  static const Color cardBackground = Color(0xFF2B3542); // Cards / containers

  // ===== Text Colors =====
  static const Color textPrimary = Color(0xFFFFFFFF); // Primary text
  static const Color textSecondary = Color(0xFFB0B7BF); // Secondary / hint text

  // ===== Accent / Controls =====
  static const Color accent = Color(0xFF3B82F6); // Slider / accent
  static const Color button = Color(0xFF3B82F6); // Button background

  // ===== Status Colors =====
  static const Color positive = Color(0xFF2ECC71); // Increase / profit

  // ===== Surfaces / Borders =====
  static const Color surfaceDark = Color(0xFF192633);
  static const Color borderDark = Color(0xFF324D67);

  static const background = Color(0xFF0F172A);
  static const card = Color(0xFF1E293B);
  static const chipBg = Color(0xFF1F2937);
  static const chipSelected = Color(0xFFFFFFFF);
  static const chipText = Color(0xFFCBD5E1);
  static const chipTextSelected = Color(0xFF0F172A);

  static const textSecondary2 = Color(0xFF94A3B8);

  // static const accent = Color(0xFF3B82F6);

  static const blue = Color(0xFF2563EB);
  static const green = Color(0xFF10B981);
  static const orange = Color(0xFFF59E0B);
  static const purple = Color(0xFF8B5CF6);

  // ===== Projection Screen Specific =====
  static const Color projectionBackground = Color(0xFF0F1720);

  // ===== CTA / Highlight =====
  static const Color primaryCta = Color(0xFF1D8CFF);
  static const Color highlightGreen = Color(0xFF00C38A);

  // ===== Charts =====
  static const Color chartInvested = Color(0xFF64748B);
  static const Color chartBaseTrack = Color(0x14FFFFFF);

  // ===== White Opacity Variants =====
  static const Color white54 = Color(0x8AFFFFFF);
  static const Color white38 = Color(0x61FFFFFF);
  static const Color white26 = Color(0x26FFFFFF);
}
